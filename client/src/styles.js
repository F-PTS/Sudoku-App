import { fontWeight } from "@mui/system"

const classes = {
    appBar: {
        borderRadius: '5px',
        margin: '30px 0',
        padding: '20px',
        display: 'flex',
        flexDirection: 'column',
        jusitfyContent: 'center',
        alignItems: 'center'
    },
    heading: {
        color: 'rgb(0,183,255)',
        fontWeight: 700,
    },
    bottomText: {
        width: '50%',
        textAlign: 'center',
        fontSize: 14
    }
}

export default classes